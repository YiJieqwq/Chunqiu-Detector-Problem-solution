{
  "name": "chunqiu-detector-solutions",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare-assets": "node scripts/prepare.mjs",
    "docs:dev": "npm run prepare-assets && vitepress dev docs",
    "docs:build": "npm run prepare-assets && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
